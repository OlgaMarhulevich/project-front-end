import React from "react";
import s from './Registration.module.scss'

export const Registration: React.FC = () => {
    return (
        <div className={s.page}>
            <div className={s.pageTitle}>Registration page</div>
        </div>
    )
}
