import React from "react";
import s from './EnterNewPassword.module.scss'

export const EnterNewPassword: React.FC = () => {
    return (
        <div className={s.page}>
            <div className={s.pageTitle}>EnterNewPassword page</div>
        </div>
    )
}
