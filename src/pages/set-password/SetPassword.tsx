import React from "react";
import s from './SetPassword.module.scss'

export const SetPassword: React.FC = () => {
    return (
        <div className={s.page}>
            <div className={s.pageTitle}>SetPassword page</div>
        </div>
    )
}
